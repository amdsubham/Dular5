rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Add your Firestore security rules here
    // This is a placeholder - configure according to your security requirements
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
